name: Docker Publish

on:
  push:
    tags:
      - "v*"
  create:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    name: Build and Publish Docker Image
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.ref == 'refs/heads/main' ||
      startsWith(github.ref, 'refs/tags/v') ||
      (github.event_name == 'create' && github.event.ref_type == 'tag' && startsWith(github.event.ref, 'v'))
    runs-on: ubuntu-latest
    env:
      DEFAULT_IMAGE_REPO: ${{ secrets.DOCKERHUB_USERNAME }}/postgram
      DEFAULT_GHCR_REPO: ghcr.io/${{ github.repository_owner }}/postgram
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve Docker Hub repository
        id: dockerhub
        shell: bash
        run: |
          set -euo pipefail
          repo='${{ vars.DOCKERHUB_REPOSITORY }}'
          if [ -z "$repo" ]; then
            repo="$DEFAULT_IMAGE_REPO"
          fi
          repo="${repo,,}"
          echo "repository=$repo" >> "$GITHUB_OUTPUT"
          echo "Using Docker Hub repository: $repo"

      - name: Resolve GHCR repository
        id: ghcr
        shell: bash
        run: |
          set -euo pipefail
          repo="$DEFAULT_GHCR_REPO"
          repo="${repo,,}"
          echo "repository=$repo" >> "$GITHUB_OUTPUT"
          echo "Using GHCR repository: $repo"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ steps.dockerhub.outputs.repository }}
            ${{ steps.ghcr.outputs.repository }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=branch
            type=ref,event=tag
            type=sha,format=short
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
